@page "/Account/Register"
@inject NavigationManager NavigationManager

@* 자체 회원가입 비활성화 — 계정은 관리자가 직접 등록/배부합니다. *@
@code {
    protected override void OnInitialized()
    {
        NavigationManager.NavigateTo("Account/Login", replace: true);
    }
}
